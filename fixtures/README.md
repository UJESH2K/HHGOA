# fixtures/ - SYNTHETIC DATA

Generated by `python -m src.fixtures.generate`. Same schema and same case ids as the organizers' dataset, ~3,000 rows instead of 590,742.

**Nothing measured here is a finding about the real data.** It exists so the whole chain - build, backend, investigation, answer files, validator - can be run and tested without the 704 MB download, and so the investigation can be asserted against planted ground truth instead of eyeballed.

Expected verdicts per planted shape are in `src/fixtures/generate.py` (`EXPECTED_VERDICT`), and asserted by `tests/test_end_to_end.py`.
